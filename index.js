const {randomFullName,randomCity, randomCityEstate, randomPerson, randomName, randomState} = require('./Generates')

console.log(randomFullName());
console.log(randomCity());
console.log(randomCityEstate());
console.log(randomPerson());
console.log(randomName(3));
console.log(randomState());